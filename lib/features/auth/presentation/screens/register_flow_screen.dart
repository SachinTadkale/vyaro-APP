/**
 * Module: Register Flow Screen
 * Purpose: Implements the Register Flow Screen module for the FarmZy mobile app.
 * Note: Documentation-only change; behavior remains unchanged.
 */
import 'package:farmzy/features/auth/presentation/screens/register_screen.dart';
import 'package:flutter/material.dart';

/**
 * Register Flow Screen.
 */
class RegisterFlowScreen extends StatelessWidget {
  const RegisterFlowScreen({super.key});

  @override
/**
 * Build.
 */
  Widget build(BuildContext context) {
    return const RegisterScreen();
  }
}
