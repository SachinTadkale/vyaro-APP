import 'package:flutter/material.dart';

class AppColors {
  // ---------- FARMER (Vibrant Green) ----------
  static const primaryFarmer = Color(0xFF2E7D32); 
  static const primaryFarmerLight = Color(0xFF81C784);
  static const accentFarmer = Color(0xFFFFC107); 

  // ---------- DELIVERY (Modern Blue) ----------
  static const primaryDelivery = Color(0xFF1565C0); 
  static const primaryDeliveryLight = Color(0xFF64B5F6);

  // ---------- GLASS SYSTEM ----------
  static const glassBackground = Color(0x1AFFFFFF); // 10% White
  static const glassBorder = Color(0x33FFFFFF); // 20% White
  static const glassGlowPrimary = Color(0x4D2E7D32); // 30% Green Glow
  static const glassGlowError = Color(0x4DBA1A1A); // 30% Red Glow

  // ---------- LIGHT THEME ----------
  static const lightBg = Color(0xFFF2F5F8);
  static const lightSurface = Color(0xFFFFFFFF);
  static const lightTextPrimary = Color(0xFF111418);
  static const lightTextSecondary = Color(0xFF5C636A);
  static const lightOutline = Color(0xFFE2E8F0);

  // ---------- DARK THEME ----------
  static const darkBg = Color(0xFF0D0F12);
  static const darkSurface = Color(0xFF16181D);
  static const darkTextPrimary = Color(0xFFF8F9FA);
  static const darkTextSecondary = Color(0xFFA1A9B3);
  static const darkOutline = Color(0xFF2D323A);

  // ---------- UTILS ----------
  static const success = Color(0xFF4CAF50);
  static const error = Color(0xFFBA1A1A);
  static const info = Color(0xFF0061A4);
  static const warning = Color(0xFFF97316);
}
